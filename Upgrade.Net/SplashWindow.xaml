<Window
    x:Class="Com.Scm.Upgrade.SplashWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="300" Width="500"
    WindowStyle="None"
    WindowStartupLocation="CenterScreen"
    Topmost="True"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="/Resources/logo.ico"
    Title="ScmDriver">
    <Border CornerRadius="5" BorderThickness="1" BorderBrush="red"
        Background="#FAFAFA">
        <Grid>
            <!-- 启动屏内容：Logo + 进度条 + 提示文本 -->
            <StackPanel x:Name="PlLogo" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20" Width="300">
                <Image Source="/Resources/logo.png" Width="128" Height="128"/>
                <TextBlock Text="我的应用" FontSize="24" TextAlignment="Center" Margin="20"/>
                <ProgressBar x:Name="progressBar" Value="100" Maximum="200" IsIndeterminate="True"/>
                <TextBlock x:Name="statusText" Text="初始化中..." Foreground="#333333" />
            </StackPanel>
            <DockPanel x:Name="PlInfo" Visibility="Collapsed" Margin="12">
                <StackPanel DockPanel.Dock="Bottom">
                    <Button x:Name="BtExit" Click="BtExit_Click">关闭</Button>
                </StackPanel>
                <Border Margin="0 0 0 6">
                    <TextBox x:Name="TbInfo" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Margin="6"></TextBox>
                </Border>
            </DockPanel>
        </Grid>
    </Border>
</Window>
